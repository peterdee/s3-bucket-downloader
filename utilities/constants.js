export const DESTINATION = `${process.cwd()}/downloads`;

export const ERRORS = {
  notAWorkerProcess: 'Not a worker process!',
};

export const MAX_KEYS = 500;

export const TIME_LABEL = 'measure';

export const WORKER_EVENTS = {
  count: 'count',
  done: 'done',
};
